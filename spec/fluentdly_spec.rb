require 'spec_helper'

describe Fluentdly do
  it 'has a version number' do
    expect(Fluentdly::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
